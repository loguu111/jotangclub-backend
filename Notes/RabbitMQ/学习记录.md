# 9.21  

## 问题回答  

### 1：除了我们刚才提到的“耗时任务等一下再处理”，通常还有什么样的情况和原因，会让一个项目引入 MQ？  

项目相对较大，加入MQ防止下游服务器崩溃导致上游服务器连带崩溃  

服务器接受了大量的请求，将请求放入MQ队列中缓冲

需要解决大量任务，使用 MQ 接收后再分配给不同服务解决  

### 2：如何使用你所选择的编程语言通过代码来向 RabbitMQ 发送消息, 并从其中接收消息？

Java 通过官方提供的RabbitMQ Java Client 库，通过添加依赖、发送消息、接收消息的步骤